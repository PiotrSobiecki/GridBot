# Nixpacks config dla głównego katalogu - wskazuje na auth-service
# UWAGA: W Railway Dashboard ustaw Root Directory na "auth-service"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd auth-service && npm ci"]

[phases.build]
cmds = []

[start]
cmd = "cd auth-service && npm start"
