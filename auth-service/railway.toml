[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Upewnij się że Railway używa Node.js 20
[build.env]
NODE_VERSION = "20"
